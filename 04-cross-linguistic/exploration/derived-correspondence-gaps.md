# فجواتُ التقابلاتِ المستخرَجةِ من الذخائر

**التاريخ:** 2026-08-06. **الطبقة:** استكشاف، لا إنشاءَ صفوفٍ ولا تعديلَ للشبكةِ المجمّدة.

## 1. حدُّ القراءة

شُغِّلت `scripts/derive_correspondences.py` بعتبةِ شاهدَين على الأزواجِ الخمسةِ التي تسمحُ بها الذخائر. الشاهدُ هنا زوجُ مدخلَين قاموسيَّين مستقلَّين لهما نصُّ المعنى الإنجليزيِّ نفسُه، وتساوى هيكلاهما الصامتيّان، واختلفا في صامتٍ واحدٍ فقط. فهو مرشّحُ انتظامٍ، لا حكمُ نسبٍ ولا إثباتُ صفٍّ.

يقول عمودُ «في الشبكة؟» واحدًا من ثلاثة:

- **نعم، صريح:** الصفُّ يسمّي الطرفَين أو فروعَهما معًا.
- **نعم، موزّع:** التقابلُ مستغرَقٌ في صفَّين يلتقيان على الانعكاس العربيّ نفسه، أو في صفّ تغيّرٍ مع صفّ هويّة. لا يوجد صفّ مباشر مستقلّ، لكن إضافةَ صفّ جديد ستكرّر علاقةً ممثَّلةً بالفعل.
- **لا:** لا صفَّ صريحًا ولا تغطيةً موزّعةً في الشبكة الحالية.

## 2. حصيلةُ كلِّ زوج

| الزوج | صيغُ الطرفِ الأوّل | صيغُ الطرفِ الثاني | المعاني المشتركة | الأزواج المستقلة ذات الفرق الواحد | التقابلات عند `min=2` |
|---|---:|---:|---:|---:|---:|
| `aramaic:hebrew` | 2,804 | 18,141 | 704 | 95 | 11 |
| `akkadian:hebrew` | 1,957 | 18,141 | 246 | 23 | 2 |
| `akkadian:aramaic` | 1,957 | 2,804 | 176 | 22 | 3 |
| `phn:hebrew` | 148 | 18,141 | 80 | 6 | 0 |
| `xpu:hebrew` | 101 | 18,141 | 56 | 4 | 0 |

## 3. جميعُ التقابلاتِ التي بلغت العتبة

| التقابل | الشواهد | أمثلةٌ من خرج الأداة | الزوج | في الشبكة؟ |
|---|---:|---|---|---|
| `ת ~ ש` | 15 | `תוב ~ שוב` «again»؛ `תמנא ~ שמונה` و`תמניא ~ שמונה` «eight» | `aramaic:hebrew` | نعم، صريح: `SIB-05` |
| `ס ~ ש` | 9 | `סערא ~ שעורה` «barley»؛ `סטנא ~ שטן` «devil»؛ `סערא ~ שיער` «hair» | `aramaic:hebrew` | نعم، صريح: `SIB-07` |
| `ד ~ ז` | 6 | `מדבחא ~ מזבח` «altar»؛ `דקנא ~ זקן` «beard»؛ `אדנא ~ אוזן` «ear» | `aramaic:hebrew` | نعم، موزّع: `DENT-03` و`DENT-04`، انعكاسا `*ḏ` |
| `נ ~ מ` | 6 | `מצרין ~ מצרים` «Egypt»؛ `חמשין ~ חמישים` «fifty»؛ `אהרון ~ ארם` «male given name» | `aramaic:hebrew` | نعم، صريح: `LIQ-02` |
| `י ~ א` | 4 | `ליא ~ לאה` «Leah»؛ `הי ~ היא` «she, it»؛ `ילף ~ אילף` «teach» | `aramaic:hebrew` | **لا** |
| `ת ~ נ` | 4 | `גבתא ~ גבינה` «cheese»؛ `רקדתא ~ רקדן` «dancer»؛ `גינתא ~ גינן` «garden» | `aramaic:hebrew` | **لا** |
| `ט ~ צ` | 3 | `עטמא ~ עצם` «bone»؛ `טביא ~ צבי` «gazelle»؛ `קיטא ~ קיץ` «summer» | `aramaic:hebrew` | نعم، صريح: `DENT-08` |
| `י ~ ה` | 2 | `ילידותא ~ הולדת` «birth»؛ `יהב ~ הבה` «give» | `aramaic:hebrew` | **لا** |
| `י ~ מ` | 2 | `יתא ~ מהות` «essence»؛ `מיא ~ מים` «water» | `aramaic:hebrew` | **لا** |
| `ת ~ א` | 2 | `חטיתא ~ חטא` «sin»؛ `חטיתא ~ חוטא` «sinner» | `aramaic:hebrew` | **لا** |
| `ת ~ ק` | 2 | `פתח ~ פקח` و`פתיחא ~ פקח` «open» | `aramaic:hebrew` | **لا** |
| `ס ~ ש` | 3 | `Kūsu ~ כוש` «Cush»؛ `Sāmerīnāya ~ שומרוני` «Samaritan»؛ `sūqum ~ שוק` «street» | `akkadian:hebrew` | نعم، موزّع: `IDN-07` مع `SIB-01` |
| `ש ~ א` | 2 | `šumma ~ אם` «if, whether»؛ `šurānum ~ אורון` «male given name» | `akkadian:hebrew` | **لا** |
| `ש ~ א` | 3 | `šurānum ~ אהרון` «male given name»؛ `šina ~ אינהו` و`šunu ~ אינהו` «they» | `akkadian:aramaic` | **لا** |
| `נ ~ ר` | 2 | `banûm ~ ברא` «create»؛ `Sîn ~ סהרא` «moon» | `akkadian:aramaic` | نعم، موزّع: `IDN-03` مع `LIQ-03`؛ لا صفَّ أكّاديًّا مباشرًا |
| `ש ~ ס` | 2 | `šumēlum ~ סמלא` «left»؛ `šaptum ~ סיפתא` «lip» | `akkadian:aramaic` | نعم، موزّع: `IDN-21` مع `SIB-07` |

## 4. الفجواتُ الصافية

النتائجُ غيرُ المغطّاةِ 8 ظهوراتٍ زوجيّة تمثّل 7 أشكال تقابل، لأنّ `ש ~ א` ظهر في زوجَين. أعلى فجوتَين `י ~ א` و`ת ~ נ`، ولكلٍّ 4 شواهد. لا فجوةَ بلغت 10 شواهد مستقلّة، ولذلك لا صفَّ جديدًا يستحقُّ الرفعَ بهذه الجولة وحدها.

أما التقابلُ الوحيدُ الذي بلغ 10 وتجاوزها فهو `ת ~ ש` في `aramaic:hebrew` وله 15 شاهدًا، لكنه ممثَّلٌ صراحةً في `SIB-05`، فلا فجوةَ فيه.

تحتاج الفجواتُ المنخفضةُ قبل أيِّ رفعٍ إلى فصلِ اللواحق والصرف وأمهاتِ القراءة والأعلام والضمائر عن تغيّر الصامت التاريخي. أوضحُ مواضع الحذر: أمثلةُ `ת ~ נ` تحمل فروقًا اشتقاقيةً ظاهرة، وأمثلةُ `ש ~ א` تضم اسمَ علم وضميرًا وأداةَ شرط.

## 5. حارسُ حلقيّاتِ الشمال

لم يظهر `ע` ولا `ח` في صفوفِ هذه الجولة عند عتبةِ شاهدَين. ويبقى الحارسُ نافذًا لكلِّ جولةٍ لاحقة: عينُ الشمالِ `ע` مجمعُ `*ʕ` و`*ġ`، فإذا قُرئت إلى العربيّة ذُكر الوجهان `ع` و`غ` معًا؛ وحاؤه `ח` مجمعُ `*ḥ` و`*ḫ`، فيُذكر الوجهان `ح` و`خ` معًا. لا يُنتخب وجهٌ لموافقة المعنى.

*English abstract:* The five supported corpus pairs yielded 16 pair-level correspondences at a minimum of two independent lexical pairs. Eight pair-level results, representing seven distinct shapes, are not explicitly or compositionally covered by the frozen network. No uncovered result reaches the promotion threshold of 10 witnesses. The only result above that threshold is Aramaic taw against Hebrew shin with 15 witnesses, already covered by SIB-05. The low-count gaps remain exploratory and require morphological, mater-lectionis, proper-name, pronoun, and loan screening before any row is proposed.
