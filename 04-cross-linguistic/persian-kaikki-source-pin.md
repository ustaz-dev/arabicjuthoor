# ورقة تثبيت لقطة Kaikki الفارسية

**التاريخ:** 2026-07-23  
**الحالة:** لقطة محلية مثبتة لفتح مسار الاسترجاع في أسبوع الغوص الكبير. هي
مصدر استرجاع فقط، لا إجازة لحكم صلة ولا دعوى تغطية للمعجم الفارسي التاريخي.

## 1. هوية اللقطة

| الحقل | القيمة المثبتة |
|---|---|
| المصدر المسمى في فهرس الذخيرة | `kaikki_persian` |
| الصفحة المسماة | `https://kaikki.org/dictionary/Persian/` |
| المسار المحلي | `Resources/persian/kaikki.org-dictionary-Persian.jsonl` |
| الحجم | `78,152,193` بايتًا |
| صفوف JSONL | `19,361` |
| رمز اللغة في كل الصفوف | `fa` |
| MD5 | `f7bdd1e5820d028fd3fbe7bfea1f1ddc` |
| SHA-256 | `434606a6b343480fcfe817c7cff34e3791b1fe5c6829703b65cb13445fc6029c` |
| الرخصة المسجلة لطبقة Kaikki في أوراق التثبيت القائمة | `CC BY-SA and GFDL` |

## 2. جرد الحقول

كل صف يحمل `word` و`lang` و`lang_code` و`pos` و`senses`. وتظهر، بحسب
المدخل، حقول `forms` و`sounds` و`etymology_text` و`etymology_templates`
و`derived` و`related` و`descendants` وحقول علاقات المعنى. من `19,361` صفًا:
يخلو `etymology_text` في `6,453`، و`forms` في `47`، و`sounds` في `5,305`.
غياب الحقل ظاهر ولا يتحول إلى معنى أو أصل مختلق.

## 3. عقد الاستخراج والتطبيع

1. يثبت المحلل الحجم والبصمتين وعدد الصفوف ورمز `fa` قبل المسح.
2. كل سطر يجب أن يكون كائن JSON؛ اختلاف الرمز أو فساد السطر يوقف البناء.
3. تقدم الرومنة المنشورة، وتبقى الكتابة الفارسية أصلًا ظاهرًا وممرًا بديلًا
   صارمًا. الرموز غير المعلنة توقف التطبيع ولا تسقط صامتة.
4. لا تنزع لواصق، ولا يعزل قرض، ولا يصدر حكم نسب في الجرد.
5. روابط `form_of` والعلاقات النصية تبني الأسر فقط؛ لا تجعل الأسرة حكمًا.

## 4. الحدود

تخلط اللقطة طبقات فارسية حديثة وتاريخية، وقروضًا ومواد معجمية غير متجانسة.
لذلك يبدأ يوم الفارسية التالي بعزل القرض العربي الوسيط من حقول الأصل المنشورة
قبل قراءة الرصيد الأصيل. لا تدخل هذه اللقطة أو طابورها خط البرهان.

## 5. تصحيح الطبقة الزمنيّة النافذ (2026-08-01)

المورد المثبّت أعلاه **معجم فارسيّة حديثة** عنوانه ورمزُه `Persian/fa`، وليس
معجمًا للفارسيّة القديمة ولا للفارسيّة الوسطى. ورودُ صورٍ من Old Persian أو
Middle Persian داخل `etymology_text` لبعض المداخل الحديثة شاهدُ اشتقاقٍ لذلك
المدخل وحده، ولا يحوّل اللقطة إلى ذخيرة تاريخيّة مستقلّة ولا يفتح مقامًا قديمًا.

- سطر النسخ: الوصف السابق «تخلط اللقطة طبقات فارسية حديثة وتاريخية» كان قد
  يوهم بتغطية طبقات معجميّة متعدّدة؛ الوصف النافذ: «لقطة فارسيّة حديثة قد تحمل
  إحالات اشتقاقية إلى صور أقدم، من غير أن تمثّل معجم تلك الصور»؛ سبب النسخ:
  منع خلط طبقة الفرع المقروء بطبقة قوائد Old/Middle Persian.
- نتيجة جرد `Resources/`: لا ملف مستقل باسم Old Persian أو Middle Persian أو
  Pahlavi أو Avestan، ولا مصدر من هذه الطبقات في `Resources/manifest.json`.
- الأثر: تبقى هذه الورقة والقراءة القائمة موسومتين `MODERN-PERSIAN`، وتُتخطّى
  قوائد الفارسية القديمة والوسطى حتى تدخل ذخيرة تاريخية مسماة. لا يُنشأ
  `readings/persian-old.md` في غياب مصدره.
