# تقرير المسار C: العبرية والآرامية، 2026-08-16

## دفعة المسبار السداسية

- الوقت: 2026-08-16، قبل بدء دفعات الإنتاج.
- المصدر: أعلى 6 صفوف `overlap` من `both` في `phonetic-sweep-hebrew.json`، وهي `both[0]` إلى `both[5]`.
- الفحص الرقمي: فُحص 6، كُتب 0، مكرر 6، ROOT-TRACE جديد 0، NUCLEUS-TRACE جديد 0، OPEN-CANDIDATE جديد 0، NO-TRACE جديد 0.
- قرار المسبار: سليم. المرشح المنشور داخل المروحة في 6 من 6، والحدث اشتغل في 6 من 6، وصفوف الصوت المسماة اكتملت في 6 من 6 بعد البحث الإلزامي في عمود الشاهد.

### `both[0]`: `כלב` ↔ `كلب`

- المروحة المرتبة: `كلب` 0.890322.
- المرشح الأفضل داخل المروحة: نعم.
- صفوف المسار: `IDN-13` في `כ↔ك`، و`IDN-04` في `ל↔ل`، و`IDN-05` في `ב↔ب`.
- درجة الحدث المختارة للفحص: 1، جذر في الـ2,285، «العض على الشيء والإمساك به شديدًا لا يفلت».
- التكرار: مكرر؛ له بطاقة محكومة في `hebrew.md`، ومنها بطاقة `klb (كلب) «الكلب»`.

### `both[1]`: `ענב` ↔ `عنب`

- المروحة المرتبة: `عنب` 0.791180، `غنب` 0.091177، `حنب` 0.002941، `ضنب` 0.000735، `نب` 0.
- المرشح الأفضل داخل المروحة: نعم.
- صفوف المسار: `IDN-15` و`SEM-03` في `ע↔ع`، و`IDN-03` في `נ↔ن`، و`IDN-05` في `ב↔ب`.
- درجة الحدث المختارة للفحص: 1، جذر في الـ2,285، «صفاء الممتلئ المستدير مع امتداد ما».
- التكرار: مكرر؛ له بطاقة محكومة في `hebrew.md`، ومنها بطاقة `ʿnb (عنب) «العنب»`.

### `both[2]`: `כל` ↔ `كل`

- المروحة المرتبة: `كل` 0.894391، ثم `كلل، كول، كيل، كال، كلو، كلي، كلا، وكل، يكل` بوزن 0.
- المرشح الأفضل داخل المروحة: نعم.
- صفوف المسار: `IDN-13` في `כ↔ك`، و`IDN-04` في `ל↔ل`.
- درجة الحدث المختارة للفحص: 2، نواة مسجلة، «تجمع الشيء كتلة دون حد أو طرف دقيق».
- التكرار: مكرر؛ له بطاقة محكومة في `hebrew.md`، ومنها بطاقة `kl (كل) «الكل والتمام»`.

### `both[3]`: `חלב` ↔ `حلب`

- المروحة المرتبة: `حلب` 0.674690، `خلب` 0.279655.
- المرشح الأفضل داخل المروحة: نعم.
- صفوف المسار: `IDN-14` و`SEM-04` في `ח↔ح`، و`IDN-04` في `ל↔ل`، و`IDN-05` في `ב↔ب`.
- درجة الحدث المختارة للفحص: 3، نزول إلى النواة، «التسيب والتفكك وما هو من لوازمه كالزيادة» مع محكمة الباء.
- التكرار: مكرر؛ له بطاقة محكومة في `hebrew.md`، ومنها بطاقة `ḥlb (حلب) «الشحم الحيواني المحرّم»`.

### `both[4]`: `כסלו` ↔ `كسل`

- المروحة المرتبة: `كسل` 0.681853، `كشل` 0.002885.
- المرشح الأفضل داخل المروحة: نعم.
- صفوف المسار: `IDN-13` في `כ↔ك`، و`IDN-07` في `ס↔س`، و`IDN-04` في `ל↔ل`.
- درجة الحدث المختارة للفحص: 1، جذر في الـ2,285، «ارتخاء الممتد الدقيق الذي شأنه أن يكون شديد التوتر».
- التكرار: مكرر؛ له بطاقة محكومة في `hebrew.md`، وهي بطاقة `kslw (كسلو) «شهر كِسليف»`.

### `both[5]`: `יוסף` ↔ `وسف`

- المروحة المرتبة: `يسف` 0.694857، `يسب` 0.025211، `يشف` 0.002940، `يشب` 0.000107، `وسف` 0.000071، `وسب` 0.000003، `وشف` 0، `وشب` 0.
- المرشح الأفضل داخل المروحة: نعم؛ ترتيبه 5، والوزن لا يحكم.
- صفوف المسار: `GLD-01` في `י↔و` أول الجذر، و`IDN-07` في `ס↔س`، و`LAB-07` أو `IDN-06` في `פ↔ف`.
- درجة الحدث المختارة للفحص: 4، محاكم الحروف للمرشح `وسف`.
- تدقيق الشبكة: المطابق السطحي لم يجد أولًا صف `י↔و` لأنه قرأ خانة الإبدال وحدها. البحث الإلزامي بالحرفين وباسم العبرية في عمود الشاهد وجد `GLD-01` وشاهده `ولد ↔ יֶלֶד`، فلم يثبت عطب في الشبكة.
- التكرار: مكرر؛ له بطاقة محكومة في `hebrew.md`، وهي بطاقة `ywsp/f (يوسف) «اسم يوسف»`.

## توقف الأداة قبل الدفعة العبرية الأولى

- الوقت: 2026-08-16، بعد سلامة المسبار وقبل كتابة أول بطاقة إنتاجية.
- العملية: جرد كل بطاقات `hebrew.md` و`aramaic.md` المحكومة، ثم مطابقتها بصور صفوف `both` لاختيار أول 50 صورة غير مكررة بترتيب `overlap` النازل.
- العطب: انتهت أداة الجرد بمهلة تنفيذ بعد 34 ثانية، رمز الخروج 124، من غير أن تعيد قائمة مكتملة.
- القرار التنفيذي: توقف فوري بلا التفاف على العطب وبلا تخمين قائمة بديلة، امتثالًا لأمر العمل.
- أثر العطب: دفعات عبرية مكتوبة 0، دفعات آرامية مكتوبة 0، بطاقات إنتاجية مكتوبة 0، إيداعات `ship.py` منفذة 0.
- آخر موضع مكتمل: المسبار العبري `both[5]`، وكل صفوفه الستة مسجلة مكررة.

