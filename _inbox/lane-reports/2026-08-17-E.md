# تقرير المسار E، الجولة اليونانية S8، الدفعة 1

- النطاق: `S8-GRC-001` إلى `S8-GRC-050` بالترتيب.
- عدد البطاقات المكتوبة: 50.
- توزيع الأحكام: `ROOT-TRACE` عدد 1؛ `NO-TRACE` عدد 42؛ `FORM-OF-ISOLATED` عدد 6؛ `OUT-OF-SCOPE` عدد 1.
- الموجب: `κρίον ↔ قرن` في حس الجعبة بوصفها وعاء.
- المروحة: 987 ظهور مرشح، تمثل 971 جذرا فريدا؛ 17 بطاقة أعادت مروحة صفر بسبب هيكل أقصر من صامتين أو أطول من 4.
- مسح المعاني: 971 جذرا بـ`--max-chars 0` في المسح الدفعي من أداة `search_arabic_root_senses.py`؛ ثبتت 1,636 مدخلة في 235 جذرا.
- الحدث: شُغلت `frozen_event.all_tiers` على الجذور الفريدة كلها؛ التغطية 971 من 971، والغياب 0. توزيع مجموعات الدرجات: `(3,4)=557`، و`(1,3,4)=41`، و`(4)=354`، و`(2,4)=16`، و`(1,4)=3`.
- أعطاب الأدوات: أوزان `rank` لكل المرشحين اليونانيين كانت `0.000000`، فبقي الترتيب متعادلا ولم يُحذف مرشح؛ وقع فشل ترميز عابر واحد في إخراج Python تحت `cp1252`، وأعيد التشغيل بـUTF-8 بلا فقد بيانات؛ لم يقع عطب في مسح المعاني أو `all_tiers`.
- فحص البطاقات: 50 عنوانا مطابقا للقائمة، لا تكرار ولا فقد، أكبر بطاقة 2,426 بايت، ولا حرف شرطة طويلة.

LANE-E DONE-S8-1 50

# تقرير المسار E، الجولة اليونانية S8، الدفعة 2

- النطاق: `S8-GRC-051` إلى `S8-GRC-100` بالترتيب.
- عدد البطاقات المكتوبة: 50.
- توزيع الأحكام: `NO-TRACE` عدد 41؛ `FORM-OF-ISOLATED` عدد 7؛ `LAW-GAP` عدد 2؛ لا حكم موجب.
- فجوتا القانون: `ὕρχη ↔ ركو` في جنس إناء المائع، وتتطلب صفا مجمدا يرخص `χ↔ك`؛ و`χάβος ↔ حبس` في أداة المنع والإمساك، وتتطلب صفا مجمدا يرخص `χ↔ح`.
- المروحة: 389 ظهور مرشح، تمثل 377 جذرا فريدا؛ 24 بطاقة أعادت مروحة صفرا بسبب هيكل أقصر من صامتين أو أطول من 4.
- مسح المعاني: 377 جذرا بـ`--max-chars 0` في المسح الدفعي من أداة `search_arabic_root_senses.py`؛ ثبتت 786 مدخلة في 115 جذرا.
- الحدث: شُغلت `frozen_event.all_tiers` على الجذور الفريدة كلها؛ التغطية 377 من 377، والغياب 0. توزيع مجموعات الدرجات: `(3,4)=277`، و`(4)=79`، و`(1,3,4)=16`، و`(2,4)=5`.
- مصفاة النقل: فُحص المرشح السالب الخاص بالمعرّب؛ لم يظهر فيه `ركو` أو `حبس` أو `كركس`. حُجب `κίρκος ↔ كركس` عن الحكم الموجب لأن وصل الحلقة بالترديد وصف شكلي غير مشهود، فصدر `NO-TRACE`.
- أعطاب الأدوات: أوزان `rank` لكل المرشحين اليونانيين كانت `0.000000`، فبقي الترتيب متعادلا ولم يُحذف مرشح؛ وقع فشل ترميز عابر واحد في طباعة فحص الحقول تحت `cp1252`، وأعيد التشغيل بـUTF-8 بلا فقد بيانات؛ لم يقع عطب في المروحة أو مسح المعاني أو `all_tiers`.
- فحص البطاقات: 50 عنوانا مطابقا للقائمة، لا تكرار ولا فقد، وكل بطاقة تحمل الحقول الإلزامية وحكما واحدا؛ أكبر بطاقة 2,497 بايت، ولا حرف شرطة طويلة.

LANE-E DONE-S8-2 50

# تقرير المسار E، الجولة اليونانية S8، الدفعة 3 والختام

- النطاق: `S8-GRC-101` إلى `S8-GRC-150` بالترتيب.
- عدد البطاقات المكتوبة في الدفعة: 50.
- توزيع أحكام الدفعة: `NO-TRACE` عدد 46؛ `FORM-OF-ISOLATED` عدد 4؛ لا حكم موجب ولا فجوة معلقة.
- مجموع الجولة كاملة: 150 بطاقة. التوزيع الكامل: `ROOT-TRACE` عدد 1؛ `NUCLEUS-TRACE` عدد 0؛ `NUCLEUS-ECHO` عدد 0؛ `FLOOR-TRACE` عدد 0؛ `LOANWORD` عدد 0؛ `NO-TRACE` عدد 129؛ `FORM-OF-ISOLATED` عدد 17؛ `LAW-GAP` عدد 2؛ `OUT-OF-SCOPE` عدد 1.
- الموجب الوحيد في الجولة: `κρίον ↔ قرن` في حس الجعبة بوصفها وعاء، من الدفعة الأولى.
- المروحة في الدفعة: 465 ظهور مرشح، تمثل 464 جذرا فريدا؛ 20 بطاقة أعادت مروحة صفرا لأن هيكلها تجاوز 4 صوامت.
- مسح المعاني: 464 جذرا بـ`--max-chars 0` في المسح الدفعي من أداة `search_arabic_root_senses.py`؛ ثبتت 791 مدخلة في 114 جذرا.
- الحدث: شُغلت `frozen_event.all_tiers` على الجذور الفريدة كلها؛ التغطية 464 من 464، والغياب 0. توزيع مجموعات الدرجات: `(3,4)=281`، و`(4)=158`، و`(1,3,4)=19`، و`(2,4)=5`، و`(1,4)=1`.
- ممر الاسترداد والتشكيك: فُحص `εὔϊχθυς ↔ كتش/حتش` فلم يثبت التقاط البذور أو القصاصات، ومعه نقلتان غير مرخصتين؛ وفُحص `ἄμαχος ↔ محص` فثبتت التنقية والجلاء واللمعان لا البياض أو البقعة، ومعه نقلتان غير مرخصتين. لذلك لم تُفتح `LAW-GAP` في الحالتين وصدر `NO-TRACE`.
- أعطاب الأدوات: أوزان `rank` لكل المرشحين اليونانيين بقيت `0.000000`، فلم يُحذف مرشح؛ لم يقع عطب في المروحة أو مسح المعاني أو `all_tiers`. شُغلت بوابة `check_nucleus_discipline.py` الكاشفة على ملفات اللغات الستة المسماة في الميثاق، فأعادت خط الأساس القائم: 227 تنبيها في 40,363 بطاقة؛ ملف `s8-greek-round.md` ليس واحدا من ملفات اللغات الستة التي تعينها البوابة، فلا تنبيه جديد من هذه الدفعة.
- فحص البطاقات: 150 عنوانا مطابقا للقائمة، و150 معرفا فريدا، لا تكرار ولا فقد؛ بطاقات الدفعة الخمسون تحمل الحقول الإلزامية وحكما واحدا، وأكبرها 1,800 بايت، ولا حرف شرطة طويلة.

LANE-E DONE-S8-3 50
