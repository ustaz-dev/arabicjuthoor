# محضر الدفعة 007 من حصاد خشيم الهنديّ الأوربيّ

**التاريخ:** 2026-08-13  
**النطاق:** 300 صف من `khashim-journey1` و`khashim-emperors`، بعد الدفعات 001 إلى 006.

## الحساب

- فُحصت 300 صف مصدر، من الصف العالمي 2907 إلى 3412.
- كُتبت 216 بطاقة جديدة من 299 صفًّا؛ وأُلحق صف واحد، `tekhne` [3266]، ببطاقة `KIE-M0154` القديمة.
- توزيع صفوف المصدر: اليونانيّة القديمة 165، والإنجليزيّة القديمة 88، واللاتينيّة القديمة 40، والنورديّة القديمة 7.
- توزيع البطاقات الجديدة: اليونانيّة القديمة 132، والإنجليزيّة القديمة 59، واللاتينيّة القديمة 22، والنورديّة القديمة 3.
- عُرضت المراوح كاملةً مرتبةً بـ`F.rank`: 4,434 مرشحًا، أوزانها جميعًا `0.000000`؛ فالوزن ترتيب لا حكم.
- استُدعي `fan_with_dialect` عند عجز الفصحى؛ لم يولد مرشحًا إضافيًّا في هذه الدفعة.
- بقيت 214 بطاقة `OPEN-CANDIDATE`؛ وكانت 139 بطاقة بلا مروحة قابلة للتوليد من الرسم المسند إلى لسانها.
- صدر أثران استكشافيان: `ROOT-TRACE` واحد و`NUCLEUS-TRACE` واحد.

## عشرة مواضع كاشفة

1. `hard` / `haard` / `hart` ↔ `حرد`: بطاقة عائلة واحدة، ومسار `h↔ح` و`r↔ر` و`d↔د` مسمّى؛ والصلابة والقسوة مدار إلى «جفاف الباطن ويبسه بذهاب الندى والطراءةِ منه»، فصدر `ROOT-TRACE` واحد.
2. `rot` «تعفّن؛ بلي» ↔ `رث`: المسار `r↔ر` و`t↔ث` مسمّى، والحدث المجمّد «مستوى من البلى» يطابق مدار المعنى؛ فصدر `NUCLEUS-TRACE`.
3. `tekhne` ↔ `تقن`: لم تُنشأ بطاقة ثانية؛ أُلحق الصف [3266] ببطاقة `KIE-M0154` لأن صورة الكلمة ومقترحها متداخلان.
4. `good` ↔ `جود / جيد`: لم يصدر موجب؛ حدث `جود` المجمّد يدور على جريان الماء، لا على الحسن العام الذي في معنى الفرع.
5. `bon` ↔ `بن / بنة`: بقي مفتوحًا؛ حدث البناء والامتداد لا يكفي وحده لمدار «جيد؛ طيب».
6. `bright` ↔ `برق`: بقي مفتوحًا؛ رسم الفرع الطويل لم يعط مسارًا مسمى كاملًا في المروحة، فلم يُستبدل مقترح المؤلف بفحص المروحة.
7. `fool` ↔ `بله / أبله`: بقي مفتوحًا؛ لم تجتمع رجل الصوت المسماة مع الحدث المجمّد في مرشح صالح.
8. `let` ↔ `ليت / لوث`: بقي مفتوحًا؛ حدث الضعف في `ليت` لا ينهض مدارًا لترك الشيء أو السماح أو التأجير.
9. `dicer` ↔ `ذكر`: بقي مفتوحًا؛ حدث `ذكر` المجمّد في النفاذ الصلب لا يساوي فعل القول بمجرده.
10. `thorax` و`xiphos`: بقيا مفتوحين بعد إسنادهما إلى اليونانيّة؛ لم تُختلق بطاقة لاتينيّة لمجرد الرومنة، ولم يُخترع مسار حين لم تولد المروحة مرشحًا صالحًا.

## سلامة الإغلاق

- كل موجب قام على الأرجل الثلاث وحدها: مسار صوتي مسمى، حدث مجمد، معنى فرع ومدار بشري مكتوب.
- لم يُستعمل غياب الصورة من اللقطة شرطًا للحكم.
- فصلت صفوف `cross-european` و`cross-linguistic` عن هذا الدور، امتثالًا لترتيب العمل المأمور به.
