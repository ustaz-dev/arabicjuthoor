# محضر حصاد الصورة المستعادة — الدفعة 001

**النطاق:** `ancestor-sweep-latin.json/both[14]–both[16]`: `qua`، `mano`، `pello`؛ كل الصفوف `direct=true`.  
**موضع البطاقات:** `04-cross-linguistic/readings/old-latin.md`.  
**قاعدة العمل:** فُتح كل عنصر في `candidates_found`، ولم يُنتخب `best` الآلي. قُرئت طبقتا الجذر والنواة مستقلتين، وشُغلت أداتا النطق والقرآن. لا تدخل فئة الحنجريّة أو موضعها في حكم.

## النتائج

| `both` | الصف | الصورة المستعادة وطبقتها | الجذر | النواة | الحكم النافذ |
|---:|---|---|---|---|---|
| 14 | `qua` | PIE `*kʷéh₂` | لا ثلاثي عربي للحس | `قع`: الطريق مشهود، والقراءة المجمدة لا تسميه | `NUCLEUS-ECHO` |
| 15 | `mano` | PIE `*meh₂-no` | `معن`: السيل مشهود، لكن `n` في `*-no-` | `مع`: الامتداد فقط | `ROOT-ECHO + NUCLEUS-ECHO` |
| 16 | `pello` | PIE `*pelh₂` | `فلخ`: الضرب نصا | `فل` لا تسمي الدفع | `ROOT-TRACE` |

## العد

- البطاقات المضافة: 3؛ البطاقات ذات موجب: 3؛ الإغلاقات: 0.
- طبقة الجذر: `ROOT-TRACE=1`، `ROOT-ECHO=1`، وبطاقة بلا موجب جذري.
- طبقة النواة: `NUCLEUS-ECHO=2`، وبطاقة بلا موجب نووي.
- كل الصور في هذه الدفعة من `Proto-Indo-European`.

## أقوى شاهد في الدفعة

`pello ~ فلخ`: *تاج العروس* «وفلّخه تفليخا: ضربه»؛ و*المحيط* «فَلَخ رأسه ضربه وشجّه، وفلّخته بالسوط ... ضربته به». فاز على `برخ` بحفظ اللام وبشاهد الضرب المباشر.

## التوقف

توقفت الدفعة عند اللاتينية `both[16]`. التالي `both[17]`، `olla`. لم يُدخل أي صف من الألسن الخمسة التالية، ولم يُشغّل Git.
