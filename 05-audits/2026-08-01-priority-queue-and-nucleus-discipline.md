# محضر قراءة طابور الأولوية وإنفاذ انضباط النواة — 2026-08-01

## النطاق

نُفذ أمر المؤلف على ملفات القراءة والاسترداد وحدها. لم يُمس خط البرهان، ولم تُشغّل أدوات مشتركة لبناء المخرجات، ولم يُستعمل git.

قُرئ قبل الحكم:

- `04-cross-linguistic/leads/priority-queue.md`
- `04-cross-linguistic/leads/gap-analysis.md`
- `04-cross-linguistic/leads/arabic-school-loan-negative.md`
- `04-cross-linguistic/leads/arabic-school-khushaim-latin.md`
- ملفات القوائد اليونانية واللاتينية والجرمانية والكلتية والإيرانية.

## فصل الطبقة الفارسية

فُحص `Resources/` وفهرسه. المورد الفارسي الوحيد المعجمي هو لقطة Kaikki للسان ذي الرمز `fa`، أي الفارسية الحديثة. لا يوجد مورد مستقل للفارسية القديمة أو الوسطى أو البهلوية أو الأفستية يصلح لإنشاء ملف قراءة تاريخي.

لذلك:

- بقي `04-cross-linguistic/readings/persian.md` موسومًا `MODERN-PERSIAN`.
- لم يُنشأ `persian-old.md`.
- أُغلقت قوائد `brātar-` و`pahan` و`gōz` بـ`HISTORICAL-STRATUM-GAP` من غير مقارنة بالحديثة.
- ثُبت الفصل نفسه في `04-cross-linguistic/persian-kaikki-source-pin.md`.

## اكتمال الطابور

| المصدر | المقروء | النتيجة الموجزة |
|---|---:|---|
| جدول خشيم المستخرج للاتينية | كله، بلا صف متروك | موجبان مستقلان فقط في الدفعة الأولى (`manus` لحاسة القوة، و`caedō` للقطع)؛ الباقي رد أو فجوة أو انتقال، مع عزل الملحق الرومانسي خارج الطبقة |
| `leads-latin` | 36/36 | صفر موجب جديد؛ أربعة مرشحين مفتوحين بعوائق مسماة؛ `taurus` مرشح انتقال غير محسوم الاتجاه |
| `leads-greek` | 82/82 | صفر موجب جديد؛ مرشحات قليلة مفتوحة بعوائق قانون/مصدر/مدار؛ انتقالات سامية محتملة معزولة |
| `leads-celtic` | 3/3 | صفر موجب؛ `selb` مرشح مفتوح؛ `mún` فجوة طبقة |
| `leads-germanic` | 3/3 | صفر موجب؛ `flēan ~ فلح` مرشح مفتوح بعوائق مسماة |
| `leads-iranian` | 3/3 | ثلاث فجوات طبقة تاريخية؛ لم تستعمل الفارسية الحديثة بدلها |

وثيقة خشيم تعرف جدولها بأنه 215 صفًا؛ والعد الآلي لسطور الجدول المستخرج يجد 217 سطرًا مع ملحقاته غير اللاتينية/الرومانسية. عولجت السطور الفعلية كلها بدل حذف الزائد لتسوية العداد.

قُرئت الكلمات الأساسية والفجوات المسماة قبل بقية الصفوف: `manus caput cor corpus costa far`، والضمائر اليونانية الخمسة، و`ik/ic/ek/hwā/þæt`، ثم `tendō` و`ὕδωρ` وصفوف الكلتية المسماة و`canna` وأدوات بومهارد الأحادية. كل قائدة تحمل اسم مقترحها ومصدرها.

## إنفاذ الإصلاحات الستة

1. أضيفت إلى القراءات الجديدة صوامت الفرع كاملة بعد الصرف، ومواضع الزوج، وسبب نزع كل صامت. الصامت الأصلي الساقط يرد البطاقة.
2. نُسخت الموجبات التي وسعت النواة إلى مدارات غير متماسكة أو استعارت معنى مادة أخرى.
3. نُسخت الجسور الدلالية المصنوعة؛ صار المدار معاني معجمية مرقمة أو فجوة.
4. مُنع فصل مادة الصورة عن مادة المعنى.
5. عُدلت مصفاة Kaikki في `scripts/recovery_pipeline/sources.py` لتقرأ `borrowed from` و`loanword` و`via` و`calque` و`ultimately from`، و`from` إذا تلاه مانح سامي مسمى. نجح `scripts/check_recovery_pipeline.py`: 47 صف اختبار و11 fixture.
6. ثُبت في الميثاق أن الحكم الصادر يحتاج نصًا حرفيًا من كل معجم عربي مسمى، لا الاسم وحده.

أضيف 17 سطر `verdict-supersession` إلى `04-cross-linguistic/data/lane_c_two_layer_semantic_promotions.jsonl`. وتشمل العينة المضادة المعلنة، ثم موجبَي الأساسي اللذين كشفهما تطبيق القاعدة نفسها: `spolium` بسبب إسقاط `l` واستعارة معنى `سلب`، و`ὄνυξ` بسبب جسر المخلب إلى الثقب وتعدد المادة.

## ترتيب الجرد

رُفع مخطط `scripts/lane_c_nucleus_reread.py` إلى v3، وصار ترتيب الجذر داخل الطبقة المعجمية:

1. الأجوف.
2. المضعف.
3. سائر المعتل.
4. السالم/الآخر.

وتبقى الطبقة الأساسية قبل المشتق المتأخر. وُسم المخرج القديم `04-cross-linguistic/data/lane_c_nucleus_reread_ranked.json` بأنه `SUPERSEDED-READING-ORDER` بدل إعادة بنائه بأداة مشتركة في هذه الجولة.

## البوابة الآلية

شُغّل بعد كل دفعة الأمر:

`python scripts/check_nucleus_discipline.py --lang ancient-greek --lang old-latin --lang gothic --lang old-norse --lang welsh --lang persian`

خط الأساس بقي 57 عيبًا ولم تضف الدفعات عيبًا جديدًا:

- `D5-FIELD`: 40
- `D3-SOURCES`: 13
- `D3-NOQUOTE`: 3
- `D5-HEADING`: 1

هذه عيوب قديمة مكشوفة لا مانعة، وبقيت كما هي لأن نطاق الجولة كان قراءة الطابور وإنفاذ النسخ على الأحكام المتغيرة، لا إعادة تحرير كامل السجل التاريخي.

## الأثر المنهجي

أضيف §8 إلى `04-cross-linguistic/exploration-charter.md`. وهو ينسخ عمليًا عبارة «لا يشترط توافق الصامت الثالث» متى كان الثالث أصليًا، ويثبت فصل الطبقات الزمنية، وترتيب الطابور والجرد، ومصفاة اتجاه النقل، وحد خشيم، والنصوص العربية الحرفية، وسطر النسخ لكل تغيير.

النتيجة المنهجية ليست رفع العائد: الصفوف غير المقروءة صارت مقروءة، وما لم يثبت بقي فجوة مسماة أو ردًا للمقارنة المنسوبة. لا يدخل أي انتقال سامي مثبت أو محتمل في بسط الوراثة.

## تصحيح طبقة الحقيقة وعقد النشر

نُقض أمر توحيد حقل الحكم والعنوان في 2026-08-01. الحقل القانوني لبطاقات هذه الطبقة هو `- الحكم (استكشاف):`، والعناوين المتنوعة تبقى بأسمائها.

- أُعيد `(استكشاف)` إلى 21 حقلًا في `aramaic.md` و64 حقلًا في `hebrew.md`.
- أُعيد الوسم نفسه إلى 85 سطر حكم في مولدات الدفعات ذات الصلة، حتى لا يعيد تشغيلها نزع طبقة الحقيقة.
- استُعيد عقد الحقول في 23 بطاقة آرامية و69 بطاقة عبرية من مادة كل بطاقة نفسها، بلا تغيير حكم لغوي أو عنوان.
- أُلغي من `check_publication_consistency.py` شرط جعل كل عنوان `### بطاقة:`؛ لم يُعَد تسمية أي عنوان.
- نتيجة `python scripts/check_publication_consistency.py` في الملفين بعد الإصلاح: الآرامية 0 مخالفة، والعبرية 0 مخالفة. المخالفات الباقية في التشغيل الكلي تخص ملفات ألسنة أخرى خارج هذا النطاق.

## عدّ الصلات بالعدّاد الواحد

الأرقام الآتية كلها مخرجات مباشرة من `python scripts/count_links.py --claims`، ولا رقم فيها من أداة أخرى.

### لقطة الأساس قبل استئناف الدفعات الجديدة

| اللسان | البطاقات: جذر | البطاقات: نواة | الدعاوى: جذر | الدعاوى: نواة |
|---|---:|---:|---:|---:|
| الآرامية | 666 | 402 | 398 | 310 |
| العبرية | 564 | 333 | 377 | 321 |

- البطاقات في المشروع: 2,538 حكمًا صادرًا.
- الدعاوى في المشروع: 1,893 أسرة متمايزة حُكم فيها.
- معامل التكرار: 1.34 بطاقة لكل دعوى.

### اللقطة الحاكمة بعد الدفعتين 014 و015

أُعيد تشغيل العدّاد نفسه بعد إيداع بطاقات المعتل والسالم. وخلال الجولة كانت ملفات ألسنة أخرى تتغير خارج هذا النطاق؛ لذلك تُحفظ اللقطتان مؤرختين ولا تُسوّى إحداهما يدويًا إلى الأخرى.

| اللسان | البطاقات: جذر | البطاقات: نواة | الدعاوى: جذر | الدعاوى: نواة |
|---|---:|---:|---:|---:|
| الآرامية | 666 | 402 | 398 | 310 |
| العبرية | 570 | 334 | 383 | 322 |

- البطاقات في المشروع: 2,546 حكمًا صادرًا.
- الدعاوى في المشروع: 1,921 أسرة متمايزة حُكم فيها.
- معامل التكرار: 1.32 بطاقة لكل دعوى.

لا يُخلط أي مجموع للبطاقات بمجموع الدعاوى؛ الدعاوى هي رقم الأسر المتمايزة، والبطاقات هي الأحكام الصادرة كما يعرفها العدّاد الواحد.

## استئناف الطابور العبري

حُفظت دفعات `יד` و`דם` و`מים` و`פה` و`לשון`، وجرد كلاين للأسماء الثنائية، وبطاقة `קום` التي تستعمل حاشية ميليتاريف في «التطور الثانوي لجذر ثنائي الصامت». ثم اكتملت دفعات بقية الأساسي وغير الأساسي في العبرية والآرامية والصفين السريانيين الاتجاهيين.

بعدها نُفذ ترتيب الجرد المأمور به:

1. الأجوف في الدفعات 007 إلى 009.
2. المضعف في الدفعات 010 إلى 013.
3. سائر المعتل في الدفعة 014: `בנה` و`קנה` و`בכה`، مع حفظ الضعيف الثالث ومنع القفز إلى النواة.
4. السالم في الدفعة 015: `זרע` و`ברך` و`פתח` و`קטל`، ونُسخت النوى التي أسقطت صامتًا أصليًا مع توفر الجذر الكامل.

في `פתח` نُقضت `بح` لسببين مستقلين: إسقاط التاء الأصلية، وأخذ الصورة من مادة `بوح` والمدار من مادة `فتح`. وفي `זרע` و`ברך` نُقض اختيار الموضعين 1 و3 لإسقاط الصامت الأوسط، وفي `קטל` نُقض إسقاط اللام.

أعيد تشغيل `python scripts/check_nucleus_discipline.py --lang aramaic --lang hebrew` بعد كل من الدفعتين 014 و015. بقي الكاشف غير مانع، ولم تظهر فيه بطاقة من الدفعتين الجديدتين؛ حصيلته النهائية بعد الدفعة 015 هي 408 ملاحظات تاريخية مكشوفة في السجل الأقدم.

## إقفال الدفعة المسمّاة للضمائر وأعضاء الجسد والفجوات

رُوجعت الكلمات التي سميت في أمر 2026-08-01 قبل أي استئناف آخر. كانت بطاقات `ik` و`ic` و`ek` و`hwā` و`þæt` و`cride` و`crú` و`mé` و`gin/giun` و`aire` و`té` و`twym` و`láech` موجودة ونافذة، فلم تكرر. كانت الوحدتان الويلزيتان الغائبتان فعلًا `craidd` و`genau`، فأضيفتا إلى `readings/welsh.md` بعد البدء بالأساسية `gên` قبل جمعها `genau`.

| الوحدة | المصير النافذ |
|---|---|
| `ik` و`ek` | `BELOW-NUCLEUS/ORIGINAL-CONSONANT-UNACCOUNTED` |
| `ic` و`hwā` | `NO-TRACE` |
| `þæt` | `SOURCE-GAP` |
| `cride` و`crú` و`mé` | `NO-TRACE` |
| `gin/giun` | `TOOL-GAP` |
| `craidd` | `SOURCE-GAP + LAW-GAP + ORIGINAL-CONSONANT-DROP` |
| `gên/genau` | `MORPHOLOGY-GAP + SOURCE-GAP + MATERIAL-UNITY-GAP` |
| `aire` | `LAW-GAP` |
| `té` و`twym` | `NO-TRACE` |
| `láech «warrior»` | `SOURCE-GAP` بعد نسخ إغلاق القرض السابق لهذا العضو وحده |

بطاقتا ويلز تحسبان صوامت الفرع كاملة، وتبدآن من الأساسية قبل الجمع، ولا تعدان الطفرة الأولية صامتًا من الساق، ولا تصدران حكمًا موجبًا. لم تغيّرا خرج العدّاد عند إضافتهما. وبعد الحكم الجذري اللاحق في الدفعة المصرية 013 صار الخرج الحاكم 2,547 بطاقة و1,922 دعوى.

أما بقية `04-cross-linguistic/leads/priority-queue.md` فهي مقفلة في محاضرها السابقة: `leads-latin` عددها 36 من 36، و`leads-greek` عددها 82 من 82، وكل من `leads-celtic` و`leads-germanic` و`leads-iranian` عددها 3 من 3. كما يثبت سجل الجرد بقاء 0 وحدة في لقطات الإنجليزية القديمة والإيرلندية القديمة والإنجليزية الوسطى. لذلك لا توجد دفعة غير مقروءة يعاد توليدها، ولا تدخل صفوف `leads/` وثيقة العرض أو عد الدليل.

## ملحق تصحيح الألسن الستة وعقد الدعوى

نُفذ بعد نقض أمر توحيد الحقول والعناوين ما يأتي في `old-latin` و`gothic` و`ancient-greek` و`old-norse` و`persian` و`welsh`:

- أُعيد `(استكشاف)` إلى كل حقل حكم كان قد نُزع منه، من غير إعادة تسمية أي عنوان.
- استُكملت معرّفات الأسرة في 21 بطاقة: 7 قوطيّة، و7 نُرديّة قديمة، و4 ويلزيّة، و3 فارسيّة.
- أُودعت النصوص الحرفية للمعاجم العربية المسماة في البطاقات الموجبة القديمة التي كشفها فاحص انضباط النواة؛ وفي `friō` استُعملت مادة `فري` نفسها للصورة والمعنى من «المحكم» و«العين».

أعطى `python scripts/check_publication_consistency.py` على المشروع كله: `RESULT: CLEAN` بعد فحص 455 نواة و27,604 كتلة بطاقة و47 صف إبدال. وأعطى فاحص انضباط النواة المقصور على الألسن الستة: `RESULT: CLEAN` بعد 17,803 بطاقات.

وأعاد `python scripts/count_links.py --claims` أرقام الألسن الستة مفصولةً:

| اللسان | بطاقات جذر | بطاقات نواة | دعاوى جذر | دعاوى نواة |
|---|---:|---:|---:|---:|
| اللاتينية القديمة | 10 | 10 | 10 | 10 |
| القوطية | 11 | 8 | 11 | 8 |
| اليونانية القديمة | 7 | 9 | 7 | 9 |
| النردية القديمة | 9 | 7 | 9 | 7 |
| الفارسية | 7 | 3 | 7 | 3 |
| الويلزية | 5 | 5 | 5 | 5 |

صفر بطاقة بلا معرّف في هذه الألسن الستة. بقيت تسع بطاقات بلا معرّف في المشروع خارج هذا النطاق: خمس مصرية وأربع قبطية.
