# محضر الدفعة 008 من حصاد خشيم الهنديّ الأوربيّ

**التاريخ:** 2026-08-13  
**النطاق:** 300 صف من `khashim-journey1` و`khashim-emperors`، بعد الدفعات 001 إلى 007.

## الحساب

- فُحصت 300 صف مصدر، من الصف العالمي 3420 إلى 4095.
- كُتبت 224 بطاقة جديدة من 297 صفًّا؛ وألحقت صفوف `philosophy` [3613] و`synchronize` [3624] و`andro` [3720] ببطاقاتها السابقة.
- توزيع صفوف المصدر: اليونانيّة القديمة 220، واللاتينيّة القديمة 61، والإنجليزيّة القديمة 15، والإيرلنديّة القديمة 3، والقوطيّة 1.
- توزيع البطاقات الجديدة: اليونانيّة القديمة 176، واللاتينيّة القديمة 31، والإنجليزيّة القديمة 14، والإيرلنديّة القديمة 2، والقوطيّة 1.
- عُرضت المراوح الموحّدة كاملةً مرتبةً بـ`F.rank`: 2,509 مرشحات، أوزانها جميعًا `0.000000`. لم تُعد مروحة `cum/com/con` مع كل صف اقتراح.
- استُدعي `fan_with_dialect` عند عجز الفصحى؛ لم يولد مرشحًا إضافيًّا.
- في البطاقات الجديدة صدر 5 آثار وبقيت 219 بطاقة `OPEN-CANDIDATE`، ومنها 185 بلا مروحة قابلة للتوليد.
- استُدرك أثر سادس في بطاقة `KIE-M0662` القديمة: `silki / silk` ↔ `سلك`. فالمحصلة 4 `ROOT-TRACE` و2 `NUCLEUS-TRACE` بين الجديد والاستدراك.

## عشرة مواضع كاشفة

1. `calcare` «حشد» ↔ `كلكل`: المروحة لا مقترح المؤلف وحده أظهرت حدث تجمع الشيء على ذاته حتى يغلظ؛ فصدر `ROOT-TRACE`.
2. `Callis` «جمع؛ حشد» ↔ `كل`: بعد ردّ التضعيف الكتابي إلى صامت واحد قام المسار؛ فصدر `NUCLEUS-TRACE`.
3. `caracalla` وأسرتها، ومعها `caracol`، ↔ `جلجل`: دُمجت صور الثوب في بطاقة واحدة، والتغطية الشاملة أقامت `ROOT-TRACE`.
4. `silki / silk` ↔ `سلك`: ظهر أنها محسوبة أصلًا في `KIE-M0662`؛ فاستُدرك الحكم في البطاقة القديمة نفسها بدل تكرارها، وصدر `ROOT-TRACE`.
5. `committee` في شاهد السابقة الجامعة ↔ `قمط`: ضم الأطراف المنتشرة إلى جماعة أقام `ROOT-TRACE`.
6. `cum / com / con` ↔ `جم`: جُمعت الصور الست من اقتراحي `جم` و`عم` في بطاقة واحدة، فصدر `NUCLEUS-TRACE` واحد.
7. `philosophy` و`synchronize` و`andro`: ألحقت الصفوف الجديدة ببطاقاتها السابقة عبر الكتب.
8. `mur / murus / murum` «جدار» ↔ `مر`: بقيت مفتوحة؛ حدث الاسترسال والحركة لا يساوي الجدار.
9. `serica` وأخواتها «حرير؛ صيني» ↔ `شرق`: بقيت مفتوحة لأن حدث طلوع الشمس لا يساوي مادة الحرير.
10. أسرة `acid` «حامض؛ حدة؛ حرارة أو ألم»: لم يُجعل مجرد ذكر الحرارة مدارًا لاشتعال `وقد`؛ بقيت مفتوحة.

## سلامة الإغلاق

- كل موجب قام على المسار الصوتي المسمّى والحدث المجمّد ومعنى الفرع والمدار البشري المكتوب، بلا شرط رابع.
- لم يُستعمل غياب الصورة من اللقطة شرطًا للحكم.
- بقيت صفوف `cross-european` و`cross-linguistic` مؤجلة إلى دورها المأمور به.
