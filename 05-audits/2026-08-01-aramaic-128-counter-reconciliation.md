# محضرُ مصالحةِ فرقِ 128 في عدِّ الآراميّة

- التاريخ: 2026-08-01.
- السؤال: لماذا تغيّر القياس من 513 صلةَ جذرٍ و428 صلةَ نواةٍ عند 06:56 إلى 641 و300 عند 08:28، مع ثبات المجموع؟
- الحكم: **لم تُرقَّ 128 بطاقةً من النواة إلى الجذر، ولم تُحذَف 128 بطاقةَ نواة. القياسان طبّقا قانونَي عدٍّ مختلفين على ملفٍ لم يتغيّر بينهما.**

## دليلُ ثباتِ الملف

- قبل إضافة هذا المحضر إلى ملف القراءة كان `aramaic.md` ذا 4,927,305 بايت، وآخر كتابةٍ له `2026-08-01 06:32:33.048 +03:00`، أي قبل القياس الأول بنحو 23 دقيقة.
- لم تقع كتابةٌ أخرى للملف بين 06:56 و08:28. بصمته قبل إلحاق محضر المصالحة: `9564B49E3ACA7EC5FCAEDB5953400010DD204BFA0A53134929C96729BCE121D2`.
- الملف الذي كُتب عند 07:30:52 كان `lane_a_coverage.jsonl` في عملية عبرية؛ حافظ على الأسطر الآرامية. وبعد المصالحة يحمل السجل 2,176 صفًا آراميًا بمعرّفات فريدة، تطابق 2,176 عضوًا في جرد `inventory-v5.sqlite`: المفقود=0، الزائد=0، المكرر=0.

## اختبارُ فرضيةِ الترقية

- في `lane_a_coverage.jsonl` لا يوجد عضو آرامي واحد حكمُه السابق نوويٌّ وحكمُه الحالي جذريٌّ موجب: العدد=0.
- الجذور الموجبة التي لم يكن `previous_outcome` فيها جذريًا عددها 21 فقط: 19 بلا حكم سابق مسجل و2 كانا `OPEN-CANDIDATE`. لذلك لا توجد كتلة ترقية مقدارها 128.
- ثلاثة أمثلة حارسة تحمل الحكمين معًا، ولم ترتفع من طبقةٍ على حساب الأخرى:
  - `kaikki_aramaic:1317:en-דחק-arc-verb-c-f3JvR6`: الجذر `ROOT-TRACE` والنواة `NUCLEUS-TRACE`.
  - `kaikki_aramaic:1749:en-צום-arc-verb-7~~8uCJm`: الجذر `ROOT-TRACE` والنواة `NUCLEUS-TRACE`.
  - `kaikki_aramaic:1768:en-תגרתא-arc-noun-estAMW1w`: الجذر `ROOT-TRACE` والنواة `NUCLEUS-TRACE`.

## توقيعُ عيبِ العد

- في 386 بطاقةً بعنوان `إعادة قراءة بطبقتين` يوجد 258 حكمًا علويًا `NUCLEUS-TRACE` و**128 حكمًا علويًا `NUCLEUS-ECHO`** بالضبط.
- فرق القياسين نقل 128 من خانة النواة إلى خانة الجذر حسابيًا، من غير أن يتغير حرفٌ في الملف. هذا يعيّن الخلل في اختزال البطاقة الثنائية إلى تصنيف واحد أو في معاملة `NUCLEUS-ECHO` داخل العداد، لا في أحكام الأعضاء.
- لا يوجد في الملفات أو المحاضر أمرُ عدٍّ محفوظ يبيّن أيَّ القياسين أساء تصنيف هذه الشريحة؛ لذلك لا أنسب إلى أداةٍ بعينها ما لم يسجله الدليل. الثابت القابل لإعادة الإنتاج أن الرقم 128 هو سكان `NUCLEUS-ECHO` في بطاقات إعادة القراءة، وأن المحتوى لم يتغير.

## الأخطاءُ الحقيقيةُ الأصغر التي كشفها التدقيق

- `kaikki_aramaic:819:en-אתא-arc-verb-NxJ~s~mm`: سُحب `NUCLEUS-TRACE` إلى `OPEN-CANDIDATE` بسبب غياب نواة مرخصة تحمل مدار المجيء؛ أثر السحب موجود في `aramaic.md`.
- `kaikki_aramaic:1667:en-תאנתא-arc-noun-f4K80FSg`: سُحب `NUCLEUS-TRACE` إلى `OPEN-CANDIDATE` لأن البطاقة القديمة لم تسمِّ نواة ثنائية من الفهرس المجمد. **كان السحب بلا سطر نسخ في `aramaic.md`، وهذا خطأ ممنوع.** أضيف أثره في هذا المحضر وفي صف العضو داخل `lane_a_coverage.jsonl`.
- السطر 1342 حافظ على `NUCLEUS-TRACE` لكنه نسخ اختيار النواة من `تن` إلى `ثن` بسبب قراءة الفهرس والمسار `DENT-01`؛ أضيف تاريخه المنظم.
- السطران 1436 و1513 سُحب منهما الحكمان الجذريان لأنهما استعملا `SIB-01` خارج نطاق الآرامية؛ الأثر النثري كان موجودًا، وأضيف تاريخه المنظم إلى صفَّي التغطية.

## القاعدةُ النافذة

- الجذر والنواة يُفحصان استقلالًا في عرض واحد، ويُعد العضو ذو الصلتين في بسط كلتيهما.
- كل تغيير حكم يضيف سجل نسخ فيه السابق والجديد والسبب والدليل والتاريخ.
- معرّف العضو المنقول إلى `lane_a_coverage.jsonl` لا يحذف بعد صدور حكم لاحق.

*English abstract:* The 128-count swing was not a 128-member judgment migration or deletion. `aramaic.md` was unchanged between the two measurements. The exact 128 signature is the set of top-level `NUCLEUS-ECHO` labels in the two-layer reread cards, showing that the measurements used incompatible exclusive counting. The audit did uncover one genuinely silent historical withdrawal, Aramaic `תאנתא` at source line 1667; its downgrade is now recorded with its prior verdict and reason. All 2,176 Aramaic member identifiers remain present and unique in the coverage ledger.
