# دليلُ العملِ في مشروعِ جُذور (يُقرأُ قبلَ أيِّ عمل)

هذا مستودعُ **arabicjuthoor.com**: دراسةُ نظامِ المعنى في اللسانِ العربيِّ (حدثُ الحرفِ ← النواةُ الثنائيّةُ ← الجذرُ الثلاثيُّ) واستكشافُ أثرِ طورِه الثنائيِّ في ألسنِ البشر. المؤلّفُ وصاحبُ القرار: ياسين تمسّك. أنتَ منفِّذٌ ومحقِّقٌ، والتجميدُ والتوقيعُ له وحدَه.

## قوانينُ الكتابةِ والإيداعِ (صارمة، لا استثناء)

1. الجوابُ للمؤلّفِ بالعربيّة. الوثائقُ عربيّةٌ مشكولةٌ بخاتمةِ *English abstract* حيثُ جرى العرف.
2. **ممنوعٌ حرفُ الشرطةِ الطويلةِ (—)** في أيِّ شيءٍ يُنشَرُ باسمِ المؤلّف. الأرقامُ غربيّةٌ (1 2 3) دائمًا حتى في العربيّة.
3. الإيداعات Conventional Commits (`feat:` `fix:` `docs:` بنطاقاتٍ مثل `feat(registry):`)، **وممنوعٌ منعًا باتًّا أيُّ نسبةٍ إلى AI** (لا Co-Authored-By ولا Generated with). الدفعُ إلى `origin/main` بعدَ كلِّ حزمةٍ منطقيّة. العملُ على main مباشرةً.
4. تحريرُ النصوصِ العربيّةِ بالبرمجةِ يمرُّ بتطبيعِ NFC للطرفَينِ (`unicodedata.normalize`) وإلّا فشلَ الاستبدالُ بصمت. الترميزُ UTF-8 دائمًا.
5. **لا قوائمَ خاصّةً معدودةً في العناوينِ والدعاوى** («19 نظيرًا» ونحوُها محظور): وحدةُ الدليلِ عندَنا معدَّلٌ منتشرٌ يُقارَنُ بخطِّ الصدفة، لا رؤوسٌ تُعَدّ. أعدادُ الفهارسِ الكاملةِ (سكّانُ الكتالوج) جائزة.
6. **التقليمُ لا الاختلاق**: كلُّ صورةٍ لغويّةٍ ومعنًى بمصدرٍ مُسمًّى؛ المرشَّحُ الساقطُ يُدوَّنُ بسببِه ولا يُمحى.
7. أسماءُ مزوّدي النماذجِ لا تظهرُ في الوثائقِ المنشورة (يُقالُ Pass 1/Pass 2).
8. طبقاتُ الحقيقةِ ثلاثٌ وتُوسَمُ دائمًا: **أدواتٌ مجمَّدة** (بتوقيعِ المؤلّف) / **تحقُّقٌ مقيس** (وحدَه يُنشَرُ رقمًا) / **استكشاف** (نوافذُ بلا أرقامَ منشورة).

## خريطةُ المستودع

- **جذرُ المستودعِ = موقعُ الويب** (GitHub Pages يخدمُ منه): `index.html` وأخواتُها والخلاصاتُ وملفّاتُ `_build_*.py`. لا تُنقَلُ من الجذر. قاعدةُ RTL في الواجهة: `direction: rtl` وحدَها، **وممنوعٌ `flex-direction: row-reverse` معها** (سبَّبَ انعكاسًا مزدوجًا).
- `01-theory/` الأصولُ النظريّة · `02-architecture/` الدستورُ والمنهج · `03-scholar-extracts/` الجداولُ والمستخرَجاتُ وملفّاتُ الحروف · `04-cross-linguistic/` الاستكشافُ عبرَ الألسن · `05-audits/` السجلُّ التجريبيُّ المؤرَّخ · `computational/` نواتجُ المحرِّكِ الداخليّ · `data/` التصديراتُ القانونيّةُ JSON · `scripts/` الأدواتُ البرمجيّة · `_inbox/` بروتوكولُ المراسلةِ بينَ الجلساتِ والمشاريع.
- `Data raw/` و`Resources/` **خارجَ git** (موادُّ خامّ): فهرسُ الذخيرةِ في `Resources/README.md` وmanifest.json (هما وحدَهما متتبَّعان).

## الأدواتُ المجمَّدةُ النافذة (لا تُعدَّلُ إلّا بتعديلٍ دستوريٍّ مؤرَّخٍ بتوقيعِ المؤلّف)

| الأداة | الملفّ | الوسم |
|---|---|---|
| دستورُ التفكيكِ العميقِ + التعديلاتُ 1 و2 و3 (قانونُ الحدثِ الكاملِ وانتخابُ الوجه) | `02-architecture/deep-decomposition-method.md` | `method-freeze-v1.0` |
| **سجلُّ الوجوهِ القانونيُّ v1.0** (29 محكمةَ حرف، مُقابَلٌ على المطبوعة) | `03-scholar-extracts/canonical-letter-registry.md` + `data/juthoor-canonical-registry.json` + `letter-dossiers/` | `registry-freeze-v1.0` |
| شبكةُ الإبدالاتِ المغلقةُ وملحقُ الصينيّةِ والسومريّة | `04-cross-linguistic/shift-network-draft.md` + `02-architecture/deep-decomposition-annex-sino-sumerian.md` | مجمَّدان |
| بطاقاتُ الحقولِ (28 نواةً رائدة) | `03-scholar-extracts/nucleus-field-cards-draft.md` | `field-cards-freeze-v1.0` |
| التصديرُ الجامعُ للمستوياتِ الثلاثة | `data/juthoor-core-levels.json` (يُبنى بـ `scripts/export_core_levels.py`) | يُعادُ بناؤه بعدَ كلِّ تعديلِ بيانات |

سجلّاتُ القراراتِ المعلَّلةِ في `05-audits/` (أهمُّها `2026-07-12-author-delegated-rulings.md` و`data/print-fasl-collation.json`)، والقديمُ المستبدَلُ محفوظٌ دائمًا في حقولِ legacy، **ولا يُحذَفُ حرفٌ من قراءاتِ جبل أبدًا** (طبقةُ إسنادٍ دائمة).

## كيف تواصلُ الاستكشافَ عبرَ الألسن (المهمّةُ المفتوحة)

1. اقرأ **الميثاق**: `04-cross-linguistic/exploration-charter.md` (ثلاثُ درجاتِ صلةٍ بحسبِ عمقِ الفرع؛ ممنوعاتُ الاختلاقِ وممنوعاتُ التخذيلِ متساويتان).
2. الوقودُ في `Resources/` (قواميسُ kaikki لاثنتَي عشرةَ لغةً وذخائرُ الجذورِ؛ الفهرسُ في README هناك).
3. افتح `04-cross-linguistic/readings/<اللغة>.md` والتزمِ بطاقةَ القراءةِ الموحَّدةَ من الميثاقِ حرفيًّا. سجلٌّ يُلحَقُ به ولا يُمحى منه.
4. الترقيةُ إلى تحقُّقٍ مقيسٍ **بإذنِ المؤلّفِ حصرًا** (عيّنةٌ ضابطةٌ عشوائيّةٌ، دستور §8).

## بواباتُ ما قبلَ «تمّ»

1. `python scripts/check_charge_purity.py` يجبُ أن يختمَ CLEAN بعدَ أيِّ مساسٍ بالقراءاتِ أو المصادرِ أو الملفّاتِ القانونيّة.
2. بعدَ تعديلِ بياناتِ المصادر: أعدْ بناءَ التصديرِ (`export_core_levels.py`) وسجلَّ md إن تغيّرَ السجلُّ JSON.
3. عملُ الواجهة: عايِنْ محليًّا (`python -m http.server` عبرَ launch «site») بالوضعَينِ الإنجليزيِّ والعربيِّ قبلَ الدفع.
4. القراراتُ الجوهريّةُ (وجهٌ جديد، شطبُ شاهد، تغييرُ قانون) تُعرَضُ على المؤلّفِ بورقةِ قرارٍ يجيبُ عنها بكلماتٍ؛ وما فوّضَه يُنفَّذُ بقرارٍ معلَّلٍ مدوَّنٍ في `05-audits/` قابلٍ للنقضِ بكلمةٍ منه.

## العلاقةُ بمشروعِ CoranIsYours

سجلُّ الوجوهِ مشتركٌ بينَ المشروعَين: عقدُ المزامنةِ في `_inbox/registry-sync-to-tafsir.md`، والوثيقةُ النافذةُ عندَهم `Tafsir Coran/methodology/16-unified-face-registry.md`. أيُّ تعديلٍ لاحقٍ على السجلِّ يمرُّ من هنا ويصلُهم بوثيقةٍ مؤرَّخةٍ مثلِها. قرارُ النوى الجوفاءِ في مصحفِهم سياديٌّ لهم.

## ما ينتظرُ كلمةَ المؤلّفِ الآن

1. تجربةُ Pass 3 بالكتيّبِ المُرسى (مُسجَّلةٌ مسبقًا في `02-architecture/pre-registration-mode-rerate.md`؛ عتبتاها كابّا 0.55 وبقاءُ OPERATE ‏60%).
2. جولةُ التحقُّقِ الكبرى بالعيّنةِ الضابطةِ العشوائيّة (دستور §8): تاجُ المشروع.
3. إعادةُ فتحِ ملفِّ الفارسيّةِ القديمةِ تحتَ الدستور، وأيُّ لغةٍ يسمّيها من ذخيرةِ Resources.
